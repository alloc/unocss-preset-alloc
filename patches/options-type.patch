From dc5d536a15d11ecdfed3372b0d863c39bbb614a9 Mon Sep 17 00:00:00 2001
From: Alec Larson <1925840+aleclarson@users.noreply.github.com>
Date: Mon, 25 Jul 2022 11:22:37 -0400
Subject: [PATCH] refactor: rename options type

---
 src/index.ts            | 2 +-
 src/variants/dark.ts    | 4 ++--
 src/variants/default.ts | 4 ++--
 src/variants/pseudo.ts  | 4 ++--
 4 files changed, 7 insertions(+), 7 deletions(-)

diff --git a/src/index.ts b/src/index.ts
index d8c0d22..79b4116 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -27,7 +27,7 @@ export interface DarkModeSelectors {
   dark?: string
 }
 
-export interface PresetMiniOptions extends PresetOptions {
+export interface Options extends PresetOptions {
   /**
    * Dark mode options
    *
diff --git a/src/variants/dark.ts b/src/variants/dark.ts
index 6a34d3c..25b1a0a 100644
--- a/src/variants/dark.ts
+++ b/src/variants/dark.ts
@@ -1,8 +1,8 @@
 import type { Variant } from '@unocss/core'
-import type { PresetMiniOptions } from '..'
+import type { Options } from '..'
 import { variantMatcher, variantParentMatcher } from '../utils'
 
-export const variantColorsMediaOrClass = (options: PresetMiniOptions = {}): Variant[] => {
+export const variantColorsMediaOrClass = (options: Options = {}): Variant[] => {
   if (options?.dark === 'class' || typeof options.dark === 'object') {
     const { dark = '.dark', light = '.light' } = typeof options.dark === 'string'
       ? {}
diff --git a/src/variants/default.ts b/src/variants/default.ts
index a266825..313e190 100644
--- a/src/variants/default.ts
+++ b/src/variants/default.ts
@@ -1,5 +1,5 @@
 import type { Variant } from '@unocss/core'
-import type { PresetMiniOptions } from '..'
+import type { Options } from '..'
 import type { Theme } from '../theme'
 import { variantBreakpoints } from './breakpoints'
 import { variantCombinators } from './combinators'
@@ -11,7 +11,7 @@ import { variantImportant } from './important'
 import { variantCustomMedia, variantPrint } from './media'
 import { partClasses, variantPseudoClassFunctions, variantPseudoClassesAndElements, variantTaggedPseudoClasses } from './pseudo'
 
-export const variants = (options: PresetMiniOptions): Variant<Theme>[] => [
+export const variants = (options: Options): Variant<Theme>[] => [
   variantVariables,
   variantCssLayer,
 
diff --git a/src/variants/pseudo.ts b/src/variants/pseudo.ts
index f9aa754..adfb3c4 100644
--- a/src/variants/pseudo.ts
+++ b/src/variants/pseudo.ts
@@ -1,6 +1,6 @@
 import type { VariantObject } from '@unocss/core'
 import { escapeRegExp } from '@unocss/core'
-import type { PresetMiniOptions } from '..'
+import type { Options } from '..'
 
 const PseudoClasses: Record<string, string> = Object.fromEntries([
   // pseudo elements part 1
@@ -159,7 +159,7 @@ export const variantPseudoClassFunctions: VariantObject = {
   autocomplete: `(${PseudoClassFunctionsStr})-(${PseudoClassesStr}|${PseudoClassesColonStr}):`,
 }
 
-export const variantTaggedPseudoClasses = (options: PresetMiniOptions = {}): VariantObject[] => {
+export const variantTaggedPseudoClasses = (options: Options = {}): VariantObject[] => {
   const attributify = !!options?.attributifyPseudo
 
   return [
-- 
2.36.1

