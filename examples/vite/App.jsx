import React from "react";
import ReactDOM from "react-dom";
import "uno.css";

ReactDOM.render(<App />, document.getElementById("app"));

function App() {
  return (
    <div className="w-inherit h-inherit">That's it. That's the tweet.</div>
  );
}
